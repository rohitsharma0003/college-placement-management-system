package com.placehub.repository;

import com.placehub.entity.EligibleBranch;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EligibleBranchRepository extends JpaRepository<EligibleBranch, Long> {
}
